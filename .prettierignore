.next/
node_modules/
coverage/
pnpm-lock.yaml
LICENSE
drizzle/
playwright-report/
blob-report/
test-results/
dist/

# Generated by next-intl from src/messages/de.json on every dev run and build.
# Formatting it starts a fight nobody wins: Prettier rewrites the quotes and
# drops the trailing commas, the next build puts them back, and `format:check`
# fails on a file nobody edited.
src/messages/*.d.json.ts

# Generated by scripts/licenses.mjs. The check that keeps it honest compares it
# byte for byte against what the lockfile implies, so a reformat by Prettier
# would fail a build that nobody broke.
THIRD-PARTY-LICENSES.md
